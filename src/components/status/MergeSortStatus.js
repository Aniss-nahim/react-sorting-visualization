import React from "react";

const MergeSortStatus = ({ animation, colorizer, arraySnapshot }) => {
  return (
    <div>
      <div>Merge sort startus Componenet</div>
    </div>
  );
};

export default MergeSortStatus;
